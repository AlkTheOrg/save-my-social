{
  "name": "save-my-social",
  "version": "0.0.9",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "concurrently \"wsrun --parallel dev\"",
    "test": "concurrently \"wsrun --parallel test\"",
    "prepare": "husky install"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^5.2.0",
    "wsrun": "^5.2.0",
    "husky": "^8.0.0"
  }
}
